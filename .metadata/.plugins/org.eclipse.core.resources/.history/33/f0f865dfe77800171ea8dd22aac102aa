package java_dzien_drugi;

import static java.lang.Math.*; // UWAGA!!! mo¿emy na p¹cztku sktryptu zaimportowaæ statyczne biblioteke Math (mog¹ byæ równie¿ inne biblioteki). Je¿eli zaimportowaliœmy na pocz¹tku biblioteke to pó¿niej w kodzie nie musimy zaczynaæ od nazwy funkcji

public class Funkcje_matematyczne {

	public static void main(String[] args) {
		
		// wykorzystanie funkcji matematycznych
		
		double y = 9.0;
		
		int x = 3;
		
		/*
		
		UWAGA!!! w tym 
		
		double pierwiastek = Math.sqrt(y); // wywo³anie fukcji matematycznych nastepuje poprzez s³owo Math i po kropce nazwa funkcji jak¹ cgcemy wykorzystaæ
		double potega = Math.pow(y, x); // w zale¿noœci od funkcji mamy rózn¹ iloœæ argumentów które musimy podaæ
		
		*/
		
		// Poniwa¿ zaimportowaliœmy biblioteke Math dla ca³ej klasy to nie musimy przed funkcjami matematycznymi u¿ywaæ nazwy Math, tylko u¿ywamy nazwy funkcji matematycznych
		
		double pierwiastek = sqrt(y);
		double potega = pow(y, x);
		
		System.out.println("Pierwiastek kwadratowy z "+ y + " wynosi: " + pierwiastek);
		System.out.println(y + " podniesiona do potêgi " + x + " wynosi: " + potega);

	}

}
